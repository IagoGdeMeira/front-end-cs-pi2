export const handleDeleteTeacher = (id) => {
    console.log("Excluir professor com ID:", id);
    
};

export const handleEditTeacher = (id) => {
    console.log("Editar professor com ID:", id);
    
};

export const handleViewDetails = (id) => {
    console.log("Visualizar detalhes do professor com ID:", id);
    
};
